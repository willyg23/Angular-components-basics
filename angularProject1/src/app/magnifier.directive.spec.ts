import { MagnifierDirective } from './magnifier.directive';

describe('MagnifierDirective', () => {
  it('should create an instance', () => {
    const directive = new MagnifierDirective();
    expect(directive).toBeTruthy();
  });
});
